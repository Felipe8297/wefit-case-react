import { styled } from '../../styles'

export const ListCardsContainer = styled('div', {
  width: '100%',
  display: 'grid',
  gridTemplateColumns: ' repeat(auto-fit, minmax(30.9rem, 1fr));',
  gap: '1.6rem',
})
